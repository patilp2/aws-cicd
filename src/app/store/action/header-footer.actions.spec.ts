import * as HeaderFooterActions from './header-footer.actions';

describe('HeaderFooter', () => {
  it('should create an instance', () => {
    expect(new HeaderFooterActions.LoadHeaderFooters()).toBeTruthy();
  });
});
