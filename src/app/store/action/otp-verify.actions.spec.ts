import * as OtpVerifyActions from './otp-verify.actions';

describe('OtpVerify', () => {
  it('should create an instance', () => {
    expect(new OtpVerifyActions.LoadOtpVerifys()).toBeTruthy();
  });
});
